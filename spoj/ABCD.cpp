#include<iostream>
#include<string.h>
#include<stdlib.h>
using namespace std;
vector<pair<char, long long > > myvector;
char nextChar(char behind,char upper)
{
	if(behind==NULL)
	{


int main()
{
	long long n;
	cin>>n;
	myvector.push_back(make_pair('A',n));
	myvector.push_back(make_pair('B',n));
        myvector.push_back(make_pair('C',n));
	myvector.push_back(make_pair('D',n));
	string str;
	cin>>str;
	char prev ;
	if((str[0]=='A')

	for(int i =0;i<str.size();i++)
	{
		if(str[i]=='A')
		{
			if(b>c)
				if(b>d)
				{
					cout<<"B";
					b--;
				}
				else
				{
					cout<<"D"
	return 0;
}
